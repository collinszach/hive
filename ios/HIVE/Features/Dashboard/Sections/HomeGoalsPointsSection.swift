import SwiftUI

/// Home · Goals & points glance (Epic H6). Placeholder — implemented in parallel.
/// See `docs/ios/HOME-SCREEN-SPEC.md` and the reference `HomeCategoriesSection`.
struct HomeGoalsPointsSection: View {
    let token: Int
    let onAuthExpired: () -> Void
    var body: some View { EmptyView() }
}
