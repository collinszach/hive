import SwiftUI

/// Home · Safe-to-Spend hero (Epic H1). Placeholder — implemented in parallel.
/// See `docs/ios/HOME-SCREEN-SPEC.md` and the reference `HomeCategoriesSection`.
struct HomeSafeToSpendSection: View {
    let token: Int
    let onAuthExpired: () -> Void
    var body: some View { EmptyView() }
}
