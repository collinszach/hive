import SwiftUI

/// Home · Net-worth pulse (Epic H4). Placeholder — implemented in parallel.
/// See `docs/ios/HOME-SCREEN-SPEC.md` and the reference `HomeCategoriesSection`.
struct HomeNetWorthSection: View {
    let token: Int
    let onAuthExpired: () -> Void
    var body: some View { EmptyView() }
}
