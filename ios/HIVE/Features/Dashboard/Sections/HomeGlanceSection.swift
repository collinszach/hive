import SwiftUI

/// Home · This month at a glance (Epic H3). Placeholder — implemented in parallel.
/// See `docs/ios/HOME-SCREEN-SPEC.md` and the reference `HomeCategoriesSection`.
struct HomeGlanceSection: View {
    let token: Int
    let onAuthExpired: () -> Void
    var body: some View { EmptyView() }
}
